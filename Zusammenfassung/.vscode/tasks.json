{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Minted",
            "type": "shell",
            "command": "pdflatex",
            "args": [
                "-shell-escape",
                "${file}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
